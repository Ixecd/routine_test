#include <bits/stdc++.h>
using namespace std;

// 构造初始化
// std::construct_at(&addr, data)在指定的内存地址上构造对象。它是为了提供一种类型安全的方式来在未初始化的内存上构造对象，特别是在需要显式控制对象的构造和析构时
// 和 placement new 非常类似
int main() {



    return 0;
}