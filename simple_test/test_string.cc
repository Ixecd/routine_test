#include <bits/stdc++.h>
using namespace std;

int main() {
    string str = "hello world!";

    cout << "str.size() = " << str.size() << endl;      // 12
    cout << "str.length() = " << str.length() << endl;  // 12

    return 0;
}